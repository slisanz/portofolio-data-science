name: CI Movie Recommender

on:
  push:
    branches: [main]
    paths:
      - "Movie-Recommender-End-to-End/**"
      - ".github/workflows/ci-movie-recsys.yml"
  pull_request:
    paths:
      - "Movie-Recommender-End-to-End/**"
      - ".github/workflows/ci-movie-recsys.yml"

defaults:
  run:
    working-directory: Movie-Recommender-End-to-End

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: Movie-Recommender-End-to-End/src/serve/requirements-serve.txt
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install ruff pytest
          pip install -r src/serve/requirements-serve.txt
      - name: Ruff
        run: ruff check src tests || true
      - name: Pytest (lightweight)
        run: pytest tests/test_evaluator.py -q
        env:
          PYTHONPATH: ${{ github.workspace }}/Movie-Recommender-End-to-End
